---
dynaconf_merge:
  true

global:
  task_argument_class_mapping:
    ingest: spark_movies_etl.tasks.task_ingest_data.IngestDataTask
    transform: spark_movies_etl.tasks.task_transform_data.TransformDataTask
  movies_regions: ["ES"]
  movies_max_reissues: 5
  data_lake:
    raw:
      base_path: tests/spark_movies_etl/integration/fixtures/data-lake-raw-test
      dataset: movies
    standardized:
      table: test.movies_standardized
    curated:
      table: test.movies_curated

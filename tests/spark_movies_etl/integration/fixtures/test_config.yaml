---
dynaconf_merge:
  true

global:
  task_argument_class_mapping:
    standardize: spark_movies_etl.tasks.task_standardize_data.StandardizeDataTask
    curate: spark_movies_etl.tasks.task_curate_data.CurateDataTask
  movie_languages_filter: ["EN"]
  data_lake:
    raw:
      base_path: tests/spark_movies_etl/integration/fixtures/data-lake-raw-test
      dataset: movie_ratings
    standardized:
      table: test.movie_ratings_standardized
    curated:
      table: test.movie_ratings_curated
